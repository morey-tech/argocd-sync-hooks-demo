# Argo CD Sync Hooks Demo
How to use [Argo CD sync hooks](https://argo-cd.readthedocs.io/en/stable/user-guide/resource_hooks/) to run validation tests after all resources in a `Healthy` state in an Application.

